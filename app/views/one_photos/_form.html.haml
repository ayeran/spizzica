= form_for @one_photo do |f|
  - if @one_photo.errors.any?
    #error_explanation
      %h2= "#{pluralize(@one_photo.errors.count, "error")} prohibited this one_photo from being saved:"
      %ul
        - @one_photo.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
