= form_for @beveragecategory do |f|
  - if @beveragecategory.errors.any?
    #error_explanation
      %h2= "#{pluralize(@beveragecategory.errors.count, "error")} prohibited this beveragecategory from being saved:"
      %ul
        - @beveragecategory.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
